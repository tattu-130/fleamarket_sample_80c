-if current_user.favorites.find_by(item_id: @item)
  = link_to item_favorites_path(@item), method: :delete do
    .favoriteBtn
      %i.fa.fa-star
        お気に入り削除
-else
  = link_to item_favorites_path(@item), method: :post do
    .favoriteBtn
      %i.fa.fa-star
        お気に入り
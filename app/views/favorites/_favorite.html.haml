-if current_user.favorites.find_by(item_id: @item)
  = link_to item_favorites_path(@item), remote: true, method: :delete do
    .favoriteBtn.favoriteBtn--color
      %i.fas.fa-heart
        いいね！
      = @favorites_count
-else
  = link_to item_favorites_path(@item), remote: true, method: :post do
    .favoriteBtn
      %i.far.fa-heart
        いいね！
      = @favorites_count


